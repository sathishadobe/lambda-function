/**
 * @typedef {object} ProcessorContext
 * @property {unknown} payload
 * @property {'staging' | 'production'} environment
 * @property {string} streamName
 * @property {string} partitionKey
 * @property {string} sequenceNumber
 */

export {};
